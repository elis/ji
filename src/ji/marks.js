export const ts = '\n' // Termination symbol
export const shortTerminator = ' ' // Short Termination Symbol
export const blockStart = '{'
export const blockEnd = '}'

export const marks = {
  ts, 
  shortTerminator, 
  blockStart,
  blockEnd
}

export default marks
